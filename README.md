# velodyne_dd-12_rs-232_controller

## A python GUI app to control a Velodyne DD-12 via the RS-232 interface

The Velodyne DD-12 supports control via an RS-232 interface.  This project builds a python GUI application to send the serial commands to the DD-12.  This is in place of the IR remote.
